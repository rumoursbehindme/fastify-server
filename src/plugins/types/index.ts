import { IIssuerOptions } from "../../lib/types";

export interface ICorePluginsOptions {
    issuerOptions: IIssuerOptions
}

export interface IClientPluginsOptions {
    issuerOptions: IIssuerOptions
}